interceptor:

  authCheck:
    interceptorName: authCheckInterceptor
    mappings:
      - /**/*
    exclude-mappings:
      - /swagger-resources
      - /swagger-resources/configuration/*
      - /swagger-ui.html
      - /webjars/springfox-swagger-ui/*
      - /test/**/*